# deploy/instances.conf — EXAMPLE instance map for deploy.sh. TRACKED FILE:
# keep only neutral example rows here. Put your real instances in
# deploy/instances.local.conf (same format, gitignored) — deploy.sh prefers it
# automatically when it exists.
#
# Format (whitespace-separated): <name>  <app-dir-on-droplet>  <port>
# <name> is also the PM2 process suffix: the process must be called trip-<name>.
trip-a  /var/www/trips/trip-a  3001
trip-b  /var/www/trips/trip-b  3002
