{
  "name": "trip-family-hub",
  "version": "1.2.0",
  "private": true,
  "description": "Family-facing launcher portal listing every trip-dashboard instance on one droplet, with one shared sign-in (family SSO)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "better-sqlite3": "^12.11.1"
  }
}
